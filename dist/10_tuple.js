"use strict";
var redColor = [255, 0, 0];
var red = redColor[0], green = redColor[1], blue = redColor[2];
console.log(red);
var randomTuple = ["hello", 9, true];
var strr = randomTuple[0], num = randomTuple[1], boo = randomTuple[2];
console.log(strr, num, boo);
